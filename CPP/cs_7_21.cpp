#include <iostream>

using namespace std;

int main() {
    int x, y, z;
    cout << "Enter a number: ";
    cin >> x;
    cout << "Enter 2 numbers: ";
    cin >> y >> z;
    cout << "Sum = " << (x+y+z);
    return 0;
}
